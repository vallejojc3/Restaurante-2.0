# Ejemplo de variables de entorno
# ⚠️ No subir este archivo con datos sensibles a repositorios públicos

# Clave secreta de la app (cambiar en producción)
SECRET_KEY=clave-secreta-por-defecto

# Cadena de conexión de la base de datos (Railway o PostgreSQL)
# Formato: postgresql://user:password@host:port/dbname
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Puerto (Railway setea $PORT automáticamente; opcional para desarrollo)
PORT=8000

# Ambiente
FLASK_ENV=production
